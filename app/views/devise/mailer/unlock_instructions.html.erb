<p><%= t('.hello_line', :email => @resource.email) %></p>

<p><%= t('.your_account_has_been_locked_line') %></p>

<p><%= t('.click_the_link_line') %></p>

<p><%= link_to t('.unlock_my_account'), unlock_url(@resource, unlock_token: @token) %></p>
